#!/usr/bin/env node
// Arquivado em .deprecados/scripts — original estava em scripts/mover-tests-para-raiz.mjs
// Conteúdo original preservado para histórico.

import { promises as fs } from 'node:fs';
import path from 'node:path';

// ...existing code preserved in archive
